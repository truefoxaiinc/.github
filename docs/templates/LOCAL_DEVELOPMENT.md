# Local development

Document supported operating systems and runtime versions, required services, dependency installation, environment setup, database migrations, seed data, startup commands, test commands and cleanup.

Use synthetic development data. Never copy production or customer data into local environments.
